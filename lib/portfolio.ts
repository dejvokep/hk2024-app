async function getPortfolioValue(_id: string): Promise<number> {
    
    return 0;
}


export async function getGraphData(_id: string, symbol: string, days: number ): Promise<Map<string, [number]>> {
    
    
    return new Map<string, [number]>();
}